rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Users collection
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
      allow read: if request.auth != null; // Allow reading other users for matching
    }
    
    // Chats collection - more permissive rules for testing
    match /chats/{chatId} {
      allow read, write: if request.auth != null;
      
      // Messages subcollection - more permissive rules
      match /messages/{messageId} {
        allow read, write: if request.auth != null;
        // Allow read access to any authenticated user for debugging
        allow read: if request.auth != null;
      }
    }
    
    // Matches collection
    match /matches/{matchId} {
      allow read, write: if request.auth != null;
    }
    
    // Pings collection
    match /pings/{pingId} {
      allow read, write: if request.auth != null;
    }
    
    // Reports collection (admin only)
    match /reports/{reportId} {
      allow read, write: if request.auth != null && 
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin';
      allow create: if request.auth != null;
    }
    
    // Requests collection (for notifications/pings)
    match /requests/{requestId} {
      allow read, write: if request.auth != null;
    }
    
    // Notifications collection
    match /notifications/{notificationId} {
      allow read, write: if request.auth != null;
    }
  }
}
